<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/navigation"
    app:startDestination="@id/movie_list">


    <fragment
        android:id="@+id/movie_list"
        android:name="com.example.androidassignment.views.FragmentMovieList"
        android:label="fragment_movie_list"
        tools:layout="@layout/fragment_movie_list"/>

   <!-- <action
        android:id="@+id/action_movie_detail"
        app:destination="@id/movie_detail" />
    <fragment
        android:id="@+id/movie_detail"
        android:name="com.example.androidassignment.views.FragmentMovieDetail"
        android:label="fragment_movie_detail"
        tools:layout="@layout/fragment_movie_detail"
        />

    <argument
        android:name="args"
        app:argType="string"
        />-->


</navigation>